# Ignore everything by default so only files intentionally placed in Python/ are tracked.
*

# Keep the repo config, README, and the managed scripts folder.
!/.gitignore
!/README.md
!/Python/
!/Python/**
